# 工作台客户端维护

这是独立客户端源码，不是业务 App 源码。业务项目可能关联任意用户目录；先通过 API 与 Workspace 记录确认实际源码路径，不假定 Projects 目录就是用户源码。

保留原工作台的全部操作，优先复用 NativeHost 中复制的原生编辑器。修改 native/ 下的生成源，不要直接修改 NativeHost 生成文件。不得修改官方 Codex 应用或私有数据库。

字号与窗口采用 Mac 原生尺寸。聊天主体 15px、侧栏 14px、次要文字 12px，不对整个客户端使用 CSS transform 或 iPad 缩放。手机画布可独立缩放。

真实额度取 account/rateLimits/read，remaining=100-usedPercent，缺失显示不可用。禁止把示例百分比当真实数据。

构建：npm run build:mac。验证：已启动服务后 npm test；界面使用电脑操作工具检查具体修改状态。保留用户的原项目，操作测试使用“工作台联调示例”。

## 后台浏览器同步窗口
桌面应用启动后提供 http://127.0.0.1:18777/mirror。使用 CUA 在隐藏的 in-app browser 打开并优先通过此页查看工作台，不向用户弹出浏览器。镜像支持点击、滚动和文字写入，操作会作用于同一个桌面工作台；它不是隔离测试副本。未支持的拖拽、系统文件选择器等操作仍使用原生 CUA。只有镜像被查看时采集画面，关闭页面后停止采集。不要使用普通首页作为原生窗口的画面替代。

此入口由 Codex 浏览器工具按需建立隐藏标签页，应用不主动创建 Codex 标签页。操作前确认项目和草稿；不能因为镜像可操作就擅自发送消息或修改项目。画面有约秒级延迟，等待反馈后再继续；失联时不得把最后一帧当成当前状态。详细使用边界见 README.md 的“给 Codex 使用的后台同步窗口”。

## 默认构建并验证修复效果

使用者已授权：完成本次请求范围内的代码修改后，默认自行执行必要的本地校验、构建、安装或加载更新，并通过隐藏的浏览器镜像检查实际修复效果，无需为这些步骤逐次请求确认。使用者当前明确要求不构建、暂不运行或自己测试时，以当前要求为准。

验证顺序：先确认修改的项目和运行版本，完成构建并让运行实例加载新版本，再用浏览器工具在后台打开 `http://127.0.0.1:18777/mirror`，进入本次修改的具体页面或状态，检查显示与交互；发现问题就继续修复并复测。工作台客户端使用 `npm run build:mac`，业务项目使用自身规定的校验和构建入口，不套用客户端命令。

重载或重启前保留用户未发送的草稿和附件，不中断正在运行的任务。镜像不支持的操作可使用原生桌面工具。构建成功不等于效果验证通过；交付时区分构建结果、实际界面验证和未验证项，不以源码检查或旧画面替代运行验证。默认授权限于当前任务的本地修改与验证，不因此自动发送模型消息或扩展任务范围。

## 当前维护基线（2026-09-22）

- 操作入口、快捷键与导出行为以 README.md 的对应章节为准；修改实现后同步更新说明与 Protocol/Authoring 模板。过时的 Ctrl 工具快捷键、Command+Q 切换模式、桌面 App名-htmlios 导出路径均不得沿用。
- 工作台通用问题在客户端或共享引擎解决，不按某个项目名称、资源名、固定坐标或截图写特判。选择、移动、字号、裁切与输入修复至少覆盖两个不同结构的样例；未做交互验证不能宣称全部项目已验证。
- 新版使用共享引擎目录，不能把含旧版本号的 Application Support 目录当旧 App 删除。清理前核对运行进程和启动入口，保留项目、布局、会话、配置与模拟器数据。
- 业务项目工作区仅限 HTMLNativeStudio；外层工程只读，除非用户明确要求导出或授权特定修改。只有明确导出才更新外层生成代码和资源，配置保持不变；导出后工作区仍可编辑。完整文件访问权限不扩大任务范围。
- 模板规范与已存在业务项目的规范分开管理：更新模板不会自动改写旧项目规则。检查旧项目时报告冲突并按用户当前授权处理，不批量覆盖其定制规范。

- 旧 GUI 构建产物 `build/studio/.../HTMLNativeStudio.app` 不属于新版运行依赖；新版后台启动不得要求它存在，headless 构建必须跳过旧 GUI。清理时只移除该 App，保留 Runtime、Studio/Shared 源码、工具和数据。

## 近期功能的维护与验收

- 启动页按钮必须反映当前项目会话，覆盖进入、关闭、切换模式、切换项目和重启；关闭恢复正常启动动作，不能丢失布局修改。
- 选区关联只传递图层身份，不转发编辑动作，不按素材名匹配。父容器变换必须覆盖选框、命中和位移逆变换；多选与单选分别验收。
- 默认转场保持短时（0.18 秒），动画临时快照在完成、中断及替换时明确清理。检查正常弹窗遮罩与动画残留的区别，禁止用项目样式覆盖掩盖渲染缺陷。
- 当前存在源码与运行引擎副本；修改前比较对应文件，保留运行副本已有能力，不用旧源码整文件覆盖。生成目录不直接编辑。交付明确区分源码包已更新、已安装、运行实例已加载、Git 已推送和发布完成。
- 文档说明实现意图，验收记录说明已观察到的结果。尚未完成的残影、复杂嵌套坐标与实际交互验证不得写成“已全部修复/已全部验证”。

## 多项目并行开发

允许在 A 项目任务运行时创建、导入或切换到 B 项目，并在不同对话中同时提交任务。每个任务使用其所属对话的工作目录、附件和运行状态；提交后台任务不自动切换共享预览。HTML/iOS 工作台仍一次预览一个项目，切换只改变显示和监听目标，不停止其他任务，切回后同步最新源码。

同一对话仍禁止重复提交；有运行或正在提交的任务时，不能重新绑定该项目的源码目录。删除项目和停止任务继续按目标项目或对话处理，不能扩大为全局操作。后台 AI 不应为验证而自动激活项目、抢占正在使用的预览。

## 项目专用模拟器

每个预览项目分配独立的模拟器 UDID，映射只保存在运行工作区，不随源码发行。运行端启动时携带项目身份，每次请求固定读取该项目，不随工作台当前选中项目改变。关闭或重启设备只操作其 UDID。项目的运行模式和页面会话分别保存，已分配设备的后台项目继续监听源码。共享构建步骤可以排队，设备安装和运行状态按项目隔离。

聊天列表允许在预览暂时离线时复用最后一次项目目录信息，预览断开不应阻断其他项目对话。项目文件路径仍需存在；模型服务本身的连接错误要单独报告，不能归因于模拟器或盲目重发消息。

模型绘制控件的 action 表示成功保存后的回调，普通点击和拖动不能触发。回调必须携带有效作品编号；缺失时拒绝事件，不得写入空编号收藏记录。恢复历史异常记录前备份，并保留绘制草稿和资源。

消息发送应先显示本地发送状态，再等待 Codex 确认；未确认不能标为已送达。已知对话的发送不等待全量侧栏同步，未选技能不扫描技能目录，标准速度不重新查询模型列表。失败保留草稿与消息身份，不自动重复提交；切换对话后回包不能清空新对话草稿。

模拟器设备隔离同时要求输入连接按项目隔离。重复激活同一项目不得关闭已有输入连接或重置模式；窗口应读取服务端模式后连接，不得在编辑模式下循环重连。关闭输入连接只按对应会话处理。

左侧 iOS 页面图层使用原生长按拖放排序，拖动期间暂停状态刷新，完成后保存 layerOrder 并支持撤销。跨容器移动需保留页面坐标、换算目标父容器坐标并检测父子循环；运行预览、跨项目和跨页面的旧拖动不得落入当前页面。实际长按和边缘滚动未验收时应明确说明。

运行态无交互动作的装饰图片、文字和背景必须透传点击；有动作的图层、输入控件、模型、滚动区域及弹窗遮罩保持交互。编辑态的选取独立处理。调整 layerOrder 后，实际命中顺序必须和绘制顺序一致。

未裁切容器的可见溢出子图层必须参与命中测试，不能让父容器 bounds 截断文案按钮的点击区域。启用裁切、隐藏或禁用的容器仍按自身约束处理；命中点通过视图坐标转换传给子图层。

## UI 原图复刻规范维护

有原图的页面任务完整遵守 engine/Protocol/Authoring/UI原图复刻规则.md（ui-replica/4）。所有页/状态必须审计、建模板、使用一致独立资源、按模板放置、抽离模板、差分及交互验收。生成素材按整体视觉验收，允许细纹理、斑点和发丝差异；替代字体不能冒称一致。规范维护不等于业务复刻授权；纯 HTML 请求不修改或构建原生工程，工作台协议及外层导出边界保持有效。

像素级升级细则见复刻规则的“像素级复刻执行门槛”：锁定原图和真实像素、独立资源验收、统一渲染参数、稳定截图、分区域校准和按授权范围分端验收。现有静态 HTML 参数校准器的使用和限制见 engine/Protocol/Authoring/自动参数校准.md；未支持工作台实时预览和 iOS 自动校准。


## 生成素材工作流（generated-assets/1）

完整执行 engine/Protocol/Authoring/生成素材规则.md：只生成图片素材，不以原件、裁切或抠图替代；完整 UI 图片输入，约束身份、姿态、轮廓、主色与材质，允许细纹理、斑点及发丝变化。检查 PNG 真实尺寸、Alpha 与主体透明度；候选回填真实页面截图验收，逐轮修正并保留回退。原始像素差分和整体视觉分别报告，布局、文字与交互不放宽。提供 inspect-asset.mjs 技术检测，不提供自动视觉审批或新增生图按钮。


## 发布前与实际安装系统核对

发布整套系统，不只打包本任务的改动。先通过运行进程与 cwd 确认实际客户端和共享引擎路径，逐文件对照运行副本、维护源码、暂存仓库和压缩包。运行副本独有功能必须合并并测试，不能直接覆盖丢失其他任务修改。对无法追溯源码的二进制明确记录，不能宣称完全一致。包内配置、凭证、项目、会话、模拟器映射和本机绝对路径不上传；安装引导的便携化差异逐项说明。保存核对报告、构建与测试记录，并从发布附件重新下载核验 SHA256 后提供地址。只有维护源码的测试通过，不代表与当前使用系统相同。
