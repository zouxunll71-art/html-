# 用户验收清单（本轮未执行）

使用 examples/NativeRules，再选择两个不同业务项目，确保没有页面名、资源名或包名特判。

1. 新建项目：393 基准；双语文件、原生导航、权限 key、前缀已生成；空白页可添加。
2. 文案：切换系统为英文/简体中文，导航、按钮、占位符、分段、参数文案对应；删除一个 key 或参数，编译错误明确；固定 HTML 中文或英文文本被拒绝。
3. 导航：在工作台左侧镜像与独立模拟器窗口分别点击原生标签切页；按钮 push 到详情隐藏标签栏；原生返回和侧滑返回两端一致；顶部栏不跟随滚动；不同项目切换不残留旧页面。
4. 链接：分别在两端点击隐私和协议，iOS 使用系统 Safari 容器；快速点击后立即切页不漏链接；点击工作台“关闭网页”后仍可继续操作；网页内容在真实模拟器窗口操作；独立 HTML 模式能查看链接。
5. 控件：输入、勾选、开关、分段、步进器、滑块的 state 绑定一致；空勾选框不自动附加协议文案；按钮 selected/disabled 及 styles 同步。
6. 加载：spinner 显隐，重复点击被禁用；delay 后清掉 loading 并导航；reset 取消待执行延时；关闭重开后未完成延时能继续；onEnter 不因滚动/重绘重复执行。
7. 弹窗：present/dismiss、关闭按钮、遮罩、双层弹窗和原生导航遮挡关系正确；输入时点空白能收键盘。
8. 资源复制：单图层/含子层；包含按钮、绑定输入、动态文案、重复列表；复制后原动作可触发，本地化和状态不被固定成快照；几何修改保留；改固定文案时导出提示回写 key。
9. 运行恢复：布局未保存时点击“运行 / 重启 iOS”；确认保存、构建、启动、安装、页面联动和画面 ACK。模拟构建失败时有阶段、日志和可关闭错误。不能删除现有 App 数据或抹掉模拟器。
10. 导出：不在当前页的页面/弹窗也存在；ExportInventory 对应全部资源；Swift 前缀一致、最低 iOS 15、Assets 与本地化齐全；无 hardcoded UI copy；关闭工作台后 App 可独立导航、输入、加载、弹窗、打开链接。
11. 导出目录与边界：独立包位于 HTMLNativeStudio/iOS/<App名>，重复导出使用编号目录。接入受支持的外层 UIKit 空工程时，首次导出迁移全部代码资源；再次导出仅更新生成内容并清除过期生成资源；检查 HostBackups 与 host-export.json。工程配置保持不变；外层生成文件被手改时停止覆盖。导出后工作区继续编辑，未再次导出前外层文件不变化。
12. 性能：约 2000 张真实图片项目；冷载与热更新、当前页资源请求、拖动、翻页和缩略图滚动分别观察；资源压缩在副本进行，原图/原工程不修改。

语法检查、声明检查、构建成功、真实交互、视觉比对分别记录。不能用此前版本的测试结果充当此版本验收。

## UI 原图复刻验收（ui-replica/1）

- 新建/安装规范后的项目包含 `UI原图复刻规则.md`，AGENTS 与默认任务提示均引导阅读；不携带某个项目的业务和资源名。
- 页面、状态、入口/返回、资源、缺失项、字体替代及坐标换算清单齐全。
- 背景覆盖全屏和安全区，等比覆盖不拉伸；正文滚动、键盘变化不带走背景，四周无白缝。导航仍由系统管理。
- 只有复杂主页制作 QA 轮廓/锚点模板；普通页面直接测量。生产资源和正常访问不加载 QA 模板或整页参考图。
- 逐页保存同尺寸、同状态的原图/实际截图对照，主要差异修复后才标为视觉完成；不把静态通过、构建通过或相似度分数当作完整验收。
- 用户限制测试的通用页面只执行允许的视觉检查；清单明确区分未实现、未验证、可见差异。

## structured/1 新增人工验收项

以下需实际运行并记录证据，不预先标记通过；默认按 AGENTS.md 在授权工作区内验证，用户限制运行时记录未验证项：
- 正常模板导入；实体文案、注释、自闭合 ui-* 和 tokens 保持原样。
- 错误字段、重复属性、不匹配标签、控件内嵌套图层均显示可定位错误，保留上次成功版本。
- 修改一个文案或动作后同步；转换检查显示源图层数及保留的手工修改层数。
- 双语文案、弹窗、原生导航和 iOS 手工改字/隐藏图层分别核对，不能误报正常编辑。
- 2000 资源项目测量同步耗时、内存与滚动表现；此次解析缓存改造未提供性能实测结论。
- 分享包在另一台电脑离线启动时能找到随包解析依赖；不依赖开发机 node_modules。

## 工作台通用交互回归

以下为验收要求，不代表当前版本已全部通过。使用联调样例，不破坏用户项目；记录版本、样例、步骤及结果。

- 启动只出现新版工作台；调整窗口、全屏后顶部工具栏和聊天输入框可见。“编辑启动页”与手机下方三个编辑按钮同排。
- Command+W 切换模式，Command+S 保存；Command+A / Command+Shift+S / Command+D 切换工具，文本焦点下保持正常编辑；Command+Q 为退出。
- Command 点击增减多选成员，每个资源独立选框；移动时不丢选区、不选中背后图层，资源与选框同步；＋ / − 同步缩放；单击空白取消选择。
- 不同项目的文字、图片、嵌套容器测试拖动、字号和边界裁切；小文案无异常大框，文字不缺字，安全区不产生额外裁切。
- 运行模式的文本和数字可输入、退格、修改，点击非输入区结束输入；观察实际延迟，不以发送事件成功替代输入结果。
- 聊天文件拖入、剪贴板图片粘贴、图片展示以及重连后已完成状态分别验证；草稿和附件不丢失，不展示内部协议 JSON，不重复刷出处理中提示。
- 镜像操作与桌面作用于同一工作区，后台查看不要求用户管理额外窗口；构建成功与镜像实际验证分开记录。

## 启动页、关联选区与动画专项回归

| 场景 | 验收要求 |
| --- | --- |
| 启动页立即/延时跳转 | 点击编辑后等待超过原延时仍停留；同一按钮关闭后恢复正常动作，布局修改不丢。切换模式、项目、重开客户端后按钮状态正确。 |
| HTML 选择对应 iOS 图层 | 点击资源和列表分别验证；只选对应成员，无匹配/隐藏/锁定不误选。移动、缩放、删除不转发到另一端。 |
| 父容器缩放和旋转 | 至少两个结构不同的样例，覆盖多级父层、组合、滚动偏移、画布缩放；比较实际可见资源与选框，点击四角、拖动、框选和多选分别验证。纯坐标测试不替代画面验证。 |
| 短时转场 | 前进、返回、Tab、弹窗显示/关闭分别检查；输入、滚动与同页同步无重复闪烁；减少动态效果开启时直接切换。 |
| 动画中断 | 弹窗/加载时立即返回或连续切页；转场结束后无旧画面、矩形半透明块和拦截点击的快照。声明的正常遮罩仍显示，关闭后消失。 |
| 导出 | 全新导出包保留默认转场并可修改参数；既有外层成品不自动变化，手动修改后的覆盖保护仍有效。 |

记录格式：版本/加载路径、样例及路由、操作步骤、预期、实际、证据、结论（通过/失败/未验证）。截至本次文档整理，动画残影修复已有构建证据，实际场景复测仍待完成；父层变换已有计算校验，复杂嵌套交互仍需按本表验收。

## iOS 优先导出回归

- 同一属性两端不同：导出节点保持当前 iOS 值，不要求先选冲突处理方式。
- 源图层已删除：不恢复旧层、不重新出现旧遮罩；暂不可见的条件层不丢覆盖。
- 默认字段噪声不被保存成新修改；自定义字段和明确清空原字体的实际修改保留。
- 导出前后工作区内容不被清空；导出记录包含策略和原差异，生成的全部页面仍需校验。
- 不因外观优先而绕过缺失资源、动作、本地化及外层手改保护。
