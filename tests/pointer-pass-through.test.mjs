import test from 'node:test';import assert from 'node:assert/strict';import vm from 'node:vm';import fs from 'node:fs';
const context=vm.createContext({});vm.runInContext(fs.readFileSync(new URL('../engine/Shared/engine.js',import.meta.url),'utf8'),context);const accepts=context.StudioEngine.acceptsPointer;
test('decoration passes clicks while editing remains selectable',()=>{for(const type of ['image','text','shape','container','nativeProgress']){const n={id:'decoration',type,fill:'#FF0000'};assert.equal(accepts(n,{},false),false);assert.equal(accepts(n,{},true),true);assert.equal(accepts(n,{action:'open'},false),true)}});
test('controls, models and modal backdrops retain interaction',()=>{for(const type of ['nativeButton','nativeTextField','nativeSwitch','scroll'])assert.equal(accepts({id:'control',type},{},false),true);assert.equal(accepts({id:'model',type:'image',modelAsset:'pottery.glb'},{},false),true);assert.equal(accepts({id:'$shade0',type:'shape'},{},false),true);assert.equal(accepts({id:'hidden',type:'nativeButton',hidden:true},{},true),false)});
